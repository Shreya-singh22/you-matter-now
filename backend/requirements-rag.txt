# Optional retrieval stack, installed on top of requirements.txt.
# Pulls torch (~500MB installed) plus an embedding model downloaded at
# runtime, so budget ~2GB of RAM. Not viable on Render's free tier.
#
# Install CPU-only torch FIRST, from its own index, or pip resolves the
# CUDA build (~2.5GB of nvidia wheels):
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-rag.txt

torch==2.13.0
langchain==1.3.18
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-huggingface==1.2.2
langchain-chroma==1.1.0
chromadb==1.5.9
sentence-transformers==6.0.0
pypdf==6.16.2
