# Pinned deliberately: an unpinned langchain pulled a 1.x release that
# removed langchain.chains and broke the chat service on deploy.
# Bump these on purpose, not by accident.

# CPU-only torch. Without this, pip installs the CUDA build on Linux
# (~2.5GB of nvidia wheels) and the deploy runs out of disk.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0

fastapi==0.141.1
uvicorn==0.52.4
SQLAlchemy==2.0.52
pydantic==2.13.5
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
python-dotenv==1.2.3
langchain==1.3.18
langchain-core==1.6.1
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-groq==1.1.3
langchain-huggingface==1.2.2
langchain-chroma==1.1.0
chromadb==1.5.9
sentence-transformers==6.0.0
langchain-classic==1.0.8
pypdf==6.16.2
