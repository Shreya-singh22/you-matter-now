# Runtime dependencies. Fits Render's free tier (512MB).
#
# Retrieval uses Chroma's bundled ONNX MiniLM, which runs on onnxruntime
# rather than torch - ~275MB resident instead of ~2GB. The index is built
# offline by build_index.py and committed, because embedding every chunk
# peaks near 700MB while embedding one question stays near 275MB.

fastapi==0.141.1
uvicorn==0.52.4
SQLAlchemy==2.0.52
pydantic==2.13.5
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
python-dotenv==1.2.3

langchain-core==1.6.1
langchain-groq==1.1.3

# Retrieval. No torch.
chromadb==1.5.9
