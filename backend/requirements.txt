# Base runtime. Small enough for a 512MB instance.
# The chatbot works with just these - it calls Groq directly.
# For retrieval-augmented answers, also install requirements-rag.txt
# (needs roughly 2GB of RAM; see render.yaml).

fastapi==0.141.1
uvicorn==0.52.4
SQLAlchemy==2.0.52
pydantic==2.13.5
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
python-dotenv==1.2.3

langchain-core==1.6.1
langchain-groq==1.1.3
